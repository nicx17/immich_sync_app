<?xml version="1.0" encoding="UTF-8"?>
<component type="desktop-application">
  <id>io.github.nicx17.mimick</id>
  <name>Mimick</name>
  <summary>A background daemon to auto-sync photos to Immich on Linux</summary>
  <metadata_license>CC0-1.0</metadata_license>
  <project_license>GPL-3.0-or-later</project_license>
  <developer id="io.github.nicx17">
    <name>Nick Cardoso</name>
  </developer>
  <description>
    <p>
      Mimick is a high-performance Rust desktop client that mirrors local media to your Immich server in real-time. 
      Designed as an interactive agent with a modern dashboard and system tray indicator, it provides transparent, 
      user-led control over your backups.
    </p>
    <ul>
      <li>Intelligent Monitoring: Uses native filesystem events with a polling fallback for sandboxed directories.</li>
      <li>Efficient Deduplication: Tracks local assets using SHA-1 checksums to prevent redundant uploads.</li>
      <li>Secure Storage: Leverages the native secret keyring standard for all configuration parameters.</li>
      <li>User-Centric UI: Provides a graphical dashboard and tray menu for manual sync, settings, and status monitoring.</li>
    </ul>
  </description>

  <screenshots>
    <screenshot type="default">
      <image>https://raw.githubusercontent.com/nicx17/mimick/main/docs/screenshots/setup_window.png</image>
      <caption>Setup Window</caption>
    </screenshot>
    <screenshot>
      <image>https://raw.githubusercontent.com/nicx17/mimick/main/docs/screenshots/control_window.png</image>
      <caption>Control Window</caption>
    </screenshot>
    <screenshot>
      <image>https://raw.githubusercontent.com/nicx17/mimick/main/docs/screenshots/queue_inspector_window.png</image>
      <caption>Queue Inspector</caption>
    </screenshot>
    <screenshot>
      <image>https://raw.githubusercontent.com/nicx17/mimick/main/docs/screenshots/about_dialog.png</image>
      <caption>About Dialog</caption>
    </screenshot>
    <screenshot>
      <image>https://raw.githubusercontent.com/nicx17/mimick/main/docs/screenshots/ping_test_screenshot.png</image>
      <caption>Ping Test Dialog</caption>
    </screenshot>
    <screenshot>
      <image>https://raw.githubusercontent.com/nicx17/mimick/main/docs/screenshots/tray_icon_screenshot.png</image>
      <caption>Tray Icon</caption>
    </screenshot>
  </screenshots>

  <launchable type="desktop-id">io.github.nicx17.mimick.desktop</launchable>
  <requires>
    <display_length compare="ge">360</display_length>
  </requires>

  <recommends>
    <control>keyboard</control>
    <control>pointing</control>
    <control>touch</control>
  </recommends>

  <url type="homepage">https://github.com/nicx17/mimick</url>
  <url type="bugtracker">https://github.com/nicx17/mimick/issues</url>
  <url type="vcs-browser">https://github.com/nicx17/mimick</url>

  <provides>
    <binary>mimick</binary>
  </provides>

  <categories>
    <category>Utility</category>
    <category>Network</category>
  </categories>
  <content_rating type="oars-1.1"/>
  <releases>
    <release version="9.4.0" date="2026-04-23">
      <description>
        <ul>
          <li>Settings: Live auto-apply for most preferences (workers, quiet hours, folder rules, per-folder album selection, watch-folders). Connectivity fields (API key and server URLs) are now applied only when explicitly saved from the Connectivity section.</li>
          <li>Single-batch sync summary notification: multiple concurrent upload workers now aggregate results and emit a single "processed" summary notification when a sync batch completes.</li>
          <li>Flatpak packaging now targets the GNOME 50 runtime for current desktop compatibility.</li>
          <li>GitHub Pages repository pipeline now dynamically generates a mimick.flatpakref file for one-click graphical installations.</li>
          <li>Logging: Console output is colorized by level and file logs use a plain, machine-friendly formatter with automatic rotation.</li>
        </ul>
      </description>
    </release>
    <release version="9.3.0" date="2026-04-14">
      <description>
        <ul>
          <li>Added a notification toggle to let users enable or disable app notifications.</li>
          <li>Switched from <code>secret-tool</code> (libsecret CLI) to the pure-Rust <code>oo7</code> crate for credential storage. Credentials are now stored in a portal-encrypted file inside the Flatpak sandbox, or in the system keyring on native installs. This fixes the <code>user interaction failed</code> error with Flatpak.</li>
          <li>The <code>.flatpakrepo</code> now includes a <code>RuntimeRepo</code> directive for automatic GNOME Platform runtime resolution on systems without Flathub pre-configured.</li>
          <li>Removed <code>libsecret</code> build dependencies and unnecessary D-Bus permissions from Flatpak manifests.</li>
          <li>Removed hardcoded <code>GSK_RENDERER=gl</code>; GTK4 now auto-selects the best renderer.</li>
          <li>Added <code>MESA_LOG_LEVEL=error</code> to Flatpak manifests to suppress harmless Mesa driver warnings.</li>
          <li>Consolidated all documentation into the project wiki and removed local markdown files.</li>
          <li>Fixed duplicate URL toggle validation handlers that could cause multiple error dialogs.</li>
          <li>Added missing config fields to diagnostics export and summary.</li>
          <li>Fixed Flatpak install failures on systems missing Flathub by default.</li>
        </ul>
      </description>
    </release>
    <release version="9.2.0" date="2026-04-14">
      <description>
        <ul>
          <li>Migrated desktop notifications from the `notify-send` system command to native `gio::Notification`, fixing the issue where notifications would silently fail inside the Flatpak sandbox.</li>
          <li>Notifications now correctly display the app's SVG icon natively via the XDG Notification Portal constraint.</li>
          <li>Removed redundant async blocking wrappers (`tokio::task::spawn_blocking`) around notification dispatches, delegating instead directly to `glib::idle_add_once`.</li>
          <li>Added `SingleMainWindow=true` to the `.desktop` launcher to better integrate with GNOME 50+ dock contexts preventing redundant "New Window" options.</li>
          <li>Cleaned up redundant branch metadata keys in the Flatpak manifests to optimize build parsing.</li>
          <li>Refined the "Startup Catch-up Mode" drop-down label length in the Settings UI so it no longer visually clips within its ComboRow box.</li>
        </ul>
      </description>
    </release>
    <release version="9.1.1" date="2026-04-10">
      <description>
        <ul>
          <li>Fixed an internal GTK focus assertion error in the folder rules configuration dialog.</li>
          <li>Fixed a bug where discarded failed uploads could leave a persistent "Pending: 1" ghost label across application restarts.</li>
          <li>Promoted application repository to correctly advertise as a stable channel instead of beta.</li>
          <li>Reconfigured the Flatpak repository builder workflow to deploy on new tag releases rather than on every push to the main branch.</li>
        </ul>
      </description>
    </release>
    <release version="9.1.0" date="2026-04-08">
      <description>
        <ul>
          <li>Expanded supported media formats to match the latest Immich server compatibility list, including AVIF, BMP, HEIF, JPEG 2000, JPEG XL, PSD, SVG, 3GPP, AVI, FLV, M4V, Matroska (MKV), MP2T, MXF, and more.</li>
          <li>The settings window now follows the desktop light and dark appearance preference at startup, and the status page uses a standard symbolic icon for more consistent rendering across icon themes.</li>
          <li>Flatpak packaging now targets the GNOME 50 runtime for current desktop compatibility.</li>
        </ul>
      </description>
    </release>
    <release version="9.0.0" date="2026-03-29">
      <description>
        <ul>
          <li>Fixed an Immich asset timestamp regression where newly uploaded files could land at the wrong timeline time or lose their intended timezone after server-side metadata processing.</li>
          <li>Upload metadata handling now preserves filesystem-based creation times more reliably and reapplies the local timezone after upload so Immich keeps the correct asset date placement.</li>
          <li>The settings window now uses <code>Status</code> and <code>Settings</code> pages, shows the first-run API-key guidance at the top of the configuration flow, and no longer forces dark mode.</li>
          <li>
            <code>Save &amp; Restart</code> has been replaced with live <code>Save Changes</code> behavior that updates the running API client, queue policy, upload worker count, and watched folders without relaunching Mimick.</li>
          <li>Watch-folder changes now reconfigure the live filesystem monitor in place, so adding or removing folders takes effect immediately after saving.</li>
        </ul>
      </description>
    </release>
    <release version="8.0.0" date="2026-03-25">
      <description>
        <ul>
          <li>Added a mobile-responsive UI refactor using <code>adw::PreferencesWindow</code> and adaptive <code>gtk::FlowBox</code> layouts for small screens.</li>
          <li>Fixed critical offline sync stability bugs ("endless loop" and false progress reporting during network outages).</li>
          <li>Introduced a health dashboard, per-folder queue status indicators, and actionable error messaging.</li>
          <li>Added new user controls for upload concurrency, quiet hours, and safer startup catch-up modes to optimize disk I/O.</li>
          <li>Improved desktop notifications with batch summaries and dedicated connectivity-lost alerts.</li>
          <li>Enhanced the album picker with a searchable modal dialog and inline album creation.</li>
        </ul>
      </description>
    </release>
    <release version="7.0.0" date="2026-03-22">
      <description>
        <ul>
          <li>Added queue inspection, retry controls, pause and resume actions, manual sync, diagnostics export, and per-folder upload rules.</li>
          <li>Uploads can now defer automatically on metered connections or while the system is running on battery power.</li>
          <li>Refined the settings window with separate Setup and Controls pages plus a persistent footer for the main actions.</li>
        </ul>
      </description>
    </release>
    <release version="6.0.0" date="2026-03-15">
      <description>
        <ul>
          <li>Added a startup catch-up scan and a persistent local sync index so previously missed media can be queued on launch without reprocessing unchanged files.</li>
          <li>Retargeted watch folders now reassociate unchanged files to the new album, and stale or deleted album mappings are refreshed automatically.</li>
          <li>Improved troubleshooting with timestamped logs and a narrower Flatpak tray permission model.</li>
        </ul>
      </description>
    </release>
    <release version="5.0.1" date="2026-03-14">
      <description>
        <ul>
          <li>Added signed <code>.flatpakrepo</code> and checksum assets to GitHub releases.</li>
          <li>Signed the published Flatpak repository metadata and embedded the public key into generated repository files.</li>
        </ul>
      </description>
    </release>
    <release version="5.0.0" date="2026-03-14">
      <description>
        <ul>
          <li>Added Run on Startup support for both Flatpak and native installs.</li>
          <li>Switched Flatpak folder access to selected directories only through the file chooser portal.</li>
          <li>Improved shutdown controls with working Save and Restart, explicit Close and Quit actions, and friendlier folder labels for portal-backed paths.</li>
        </ul>
      </description>
    </release>
    <release version="4.0.0" date="2026-03-14">
      <description>
        <p>Complete rewrite in Rust + GTK4 + Libadwaita. Replaces the Python/PySide6 implementation with a lean, memory-safe daemon. Binary size drops from ~80MB to ~2MB. Concurrent upload workers, in-memory shared state, persistent file logging, and tray via StatusNotifierItem.</p>
      </description>
    </release>
  </releases>
</component>
